name: example
description: "An example of segmentedUI"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  segmented_ui:
    path: ../.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
